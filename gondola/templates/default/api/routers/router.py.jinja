from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

from core.database import get_db
{% if model_name %}
from api.models.{{ file_name }} import {{ model_name }}
from api.models.schemas.{{ file_name }} import (
    {{ model_name }}Create,
    {{ model_name }}Update,
    {{ model_name }}Read,
)
{% endif %}

router = APIRouter()


@router.get("/")
async def list_{{ plural_name }}(
    session: AsyncSession = Depends(get_db),
    skip: int = 0,
    limit: int = 100,
):
    """List {{ plural_name }}."""
    return {"items": [], "total": 0}


{% if model_name %}
@router.post("/", response_model={{ model_name }}Read, status_code=status.HTTP_201_CREATED)
async def create_{{ file_name }}(
    payload: {{ model_name }}Create,
    session: AsyncSession = Depends(get_db),
):
    """Create {{ file_name }}."""
    raise HTTPException(status_code=status.HTTP_501_NOT_IMPLEMENTED, detail="Not implemented")


@router.get("/{item_id}", response_model={{ model_name }}Read)
async def get_{{ file_name }}(
    item_id: str,
    session: AsyncSession = Depends(get_db),
):
    """Get {{ file_name }} by id."""
    raise HTTPException(status_code=status.HTTP_501_NOT_IMPLEMENTED, detail="Not implemented")


@router.put("/{item_id}", response_model={{ model_name }}Read)
async def update_{{ file_name }}(
    item_id: str,
    payload: {{ model_name }}Update,
    session: AsyncSession = Depends(get_db),
):
    """Update {{ file_name }}."""
    raise HTTPException(status_code=status.HTTP_501_NOT_IMPLEMENTED, detail="Not implemented")


@router.delete("/{item_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_{{ file_name }}(
    item_id: str,
    session: AsyncSession = Depends(get_db),
):
    """Delete {{ file_name }}."""
    raise HTTPException(status_code=status.HTTP_501_NOT_IMPLEMENTED, detail="Not implemented")
{% endif %}
