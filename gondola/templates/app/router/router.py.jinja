from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select

from core.database import get_session
{% if model_name %}
from app.models.{{ file_name }} import {{ model_name }}
from app.models.serializers.{{ file_name }}_serializer import (
    {{ model_name }}Create,
    {{ model_name }}Update,
    {{ model_name }}Response,
)
{% endif %}

router = APIRouter()

@router.get("/")
async def list_{{ plural_name }}(
    session: AsyncSession = Depends(get_session),
    skip: int = 0,
    limit: int = 100,
):
    """List all {{ plural_name }}."""
    # Implement your logic here
    return {"items": [], "total": 0}

@router.post("/", response_model={{ model_name }}Response, status_code=status.HTTP_201_CREATED)
async def create_{{ file_name }}(
    {{ file_name }}_data: {{ model_name }}Create,
    session: AsyncSession = Depends(get_session),
):
    """Create a new {{ file_name }}."""
    # Implement your logic here
    pass

@router.get("/{id}", response_model={{ model_name }}Response)
async def get_{{ file_name }}(
    id: str,
    session: AsyncSession = Depends(get_session),
):
    """Get {{ file_name }} by ID."""
    # Implement your logic here
    pass

@router.put("/{id}", response_model={{ model_name }}Response)
async def update_{{ file_name }}(
    id: str,
    {{ file_name }}_data: {{ model_name }}Update,
    session: AsyncSession = Depends(get_session),
):
    """Update {{ file_name }}."""
    # Implement your logic here
    pass

@router.delete("/{id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_{{ file_name }}(
    id: str,
    session: AsyncSession = Depends(get_session),
):
    """Delete {{ file_name }}."""
    # Implement your logic here
    pass
